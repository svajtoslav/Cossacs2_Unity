// V396A7R2
// The experimental V396A7 parallel framebuffer/XML parser has been retired.
// This intentionally empty source file overwrites the old A7 partial renderer so
// extracting this patch is enough; no manual deletion step is required.
